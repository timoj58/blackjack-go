apiVersion: v1
kind: Service
metadata:
  name: {{ include "blackjack.fullname" . }}
  labels:
    {{- include "blackjack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blackjack.selectorLabels" . | nindent 4 }}
