apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blackjack.fullname" . }}-test-connection"
  labels:
    {{- include "blackjack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blackjack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
